# Tests for the pay dates application

## Requirements

* composer

## First time

Before running the tests, you'll need to install the composer packages:
```shell
composer install
```

## Run tests

Run:
```shell
./vendor/bin/phpunit tests
```